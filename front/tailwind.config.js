/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // This is a common pattern for Angular projects
    // You might need to add other paths if you have components outside of src/
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
