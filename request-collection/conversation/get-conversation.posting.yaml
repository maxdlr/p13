name: get conversation
method: POST
url: http://localhost:8080/graphql
body:
  content: |-
    {
      "query": "query GetConversation { GetConversation(id: 1) { id wsTopic }}"
    }
  content_type: application/json
headers:
- name: content-type
  value: application/json
